import { IKVStore } from './KVStore';
export declare class PrefixStore implements IKVStore {
    private base;
    prefix: Uint8Array;
    constructor(base: IKVStore, prefix: Uint8Array);
    private _prefixKey;
    get(key: Uint8Array): Uint8Array;
    has(key: Uint8Array): boolean;
    set(key: Uint8Array, value: Uint8Array): void;
    delete(key: Uint8Array): void;
    iterator(start: Uint8Array, end: Uint8Array): Iterable<[Uint8Array, Uint8Array]>;
    reverseIterator(start: Uint8Array, end: Uint8Array): Iterable<[Uint8Array, Uint8Array]>;
}
